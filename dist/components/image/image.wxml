<image
  src="{{ show ? src : defaultSrc }}"
  mode="{{ show ? mode : 'aspectFit' }}"
  webp="{{ webp }}"
  lazy-load="{{ lazyLoad }}"
  show-menu-by-longpress="{{ showMenuByLongpress }}"
  style="{{ customStyle }}"
  class="custom-class"
  bind:error="bindError"
  bind:load="bindLoad"
></image>
